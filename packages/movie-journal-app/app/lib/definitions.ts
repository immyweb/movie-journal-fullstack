export type Movie = {
  id: string;
  dateWatched: string;
  film: string;
  review: string;
  rating: number;
  like: boolean;
};

export type User = {
  id: string;
  name: string;
  email: string;
  password: string;
};
